{
  "name": "@sde/core",
  "version": "0.0.1",
  "main": "lib/index.js",
  "types": "types/index.d.ts",
  "private": true,
  "scripts": {
    "build": "tsc -p ./tsconfig.prod.json",
    "test": "run-s build jest",
    "clean": "rimraf node_modules lib types",
	"jest": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "jest": "^24.8.0",
    "npm-run-all": "^4.1.5",
    "rimraf": "^2.7.1",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.3"
  }
}
